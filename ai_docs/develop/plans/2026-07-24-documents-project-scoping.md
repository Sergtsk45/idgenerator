# План: Разделение документов на проектные и глобальные (scoping по объекту)

**Создан:** 2026-07-24
**Статус:** Завершено локально; rollout на staging/production требует доступа к окружениям
**Приоритет:** Высокий

---

## Цель

Сделать так, чтобы на экране документов текущего объекта были видны **только его проектные документы + все глобальные**, а не весь реестр. Дать пользователю понятный интерфейс с тремя режимами просмотра (**Проект / Глобальные / Все**) и полным набором действий (создание, редактирование, удаление) прямо в реестре.

## Контекст

### Текущее поведение (проблема)
- Таблица `documents` **не привязана к объекту**: «проектность» определяется только через `document_bindings`.
- `GET /api/documents` (`server/storage.ts::searchDocuments`) отдаёт **весь реестр** без фильтра по объекту.
- Поле `scope` (`project`/`global`) — просто метка, на выдачу не влияет.
- Итог: в новом объекте видны «чужие» документы старых объектов.

### Принятые решения (согласованы с заказчиком)
1. **Модель:** добавляем `object_id` в `documents` (источник истины на уровне записи).
2. **Связь с глобальными:** Вариант 1 «Ссылка» — глобальный документ привязывается к материалам любого объекта без копирования (один документ → много bindings).
3. **UI:** три сегмента — **Проект / Глобальные / Все** (в режиме «Все» карточки помечаются бейджем `Проект` / `Глобальный`).
4. **Осиротевшие `project`-документы** без bindings при backfill: **soft-delete** (`deleted_at = now()`).
5. **Права на глобальные документы:** редактировать/удалять/менять scope может **любой владелец** (без ограничения ролью/создателем).
6. **Счётчик «Документы качества»** на `/source-data`: **учитывает** проектные текущего объекта **+ глобальные**.
7. **Действие «Сделать глобальным»:** **доступно всем** пользователям.

### Правило видимости
```
показываем документы, где deleted_at IS NULL И (
  scope = 'global'                                  -- глобальные видны везде
  OR object_id = :currentObjectId                   -- проектные только в своём объекте
)
```

---

## Архитектурные изменения (кратко)

- `documents.object_id integer NULL references objects(id) on delete cascade`
  - `scope='project'` ⇒ `object_id = <объект>` (NOT NULL по смыслу)
  - `scope='global'` ⇒ `object_id = NULL`
- Согласованность гарантируется CHECK-ограничением:
  `(scope='global' AND object_id IS NULL) OR (scope='project' AND object_id IS NOT NULL)`
- `searchDocuments` принимает `objectId` и `viewMode` (`project|global|all`).
- Глобальные удаляются только если не используются в актах (уже есть `act_document_attachments ON DELETE RESTRICT`).

---

## Задачи

### Задача: DOC-SCOPE-001 — Миграция БД: добавить object_id в documents
- **Статус**: Реализована
- **Приоритет**: Высокий
- **Описание**: Добавить колонку `object_id`, индекс, CHECK-ограничение согласованности scope↔object_id. Безопасная миграция: добавление → backfill → ужесточение.
- **Шаги выполнения**:
  - [x] Создать `migrations/00XX_documents_object_id.sql`
  - [x] `ALTER TABLE documents ADD COLUMN object_id integer` (nullable на этом шаге)
  - [x] FK `references objects(id) ON DELETE CASCADE`
  - [x] Индекс `documents_object_id_idx` и составной `documents_scope_object_idx (scope, object_id)`
  - [x] Backfill: проставить `object_id` для `scope='project'` по bindings (материал → object_id, либо `document_bindings.object_id`)
  - [x] Документы `scope='project'` без единой привязки (осиротевшие) → `deleted_at = now()` (решение №4)
  - [x] Добавить CHECK `documents_scope_object_check`
  - [x] Проверить прогон в `.github/workflows/test-migrations.yml`
    - 2026-07-24: `0019_drop_legacy_telegram_columns.sql` получил безопасный backfill orphan-объектов через заблокированного legacy-владельца; workflow расширен проверками `0026`/`0027`.
- **Зависимости**: нет
- **Проверка**: миграция применяется на чистой и на текущей БД без ошибок; данные не потеряны

### Задача: DOC-SCOPE-002 — Обновить схему Drizzle и Zod-контракты
- **Статус**: Реализована
- **Приоритет**: Высокий
- **Описание**: Отразить `object_id` в `shared/schema.ts`; обновить `shared/routes.ts` (list с фильтром по объекту/режиму, create с object_id, новый patch).
- **Шаги выполнения**:
  - [x] `shared/schema.ts`: добавить `objectId` в `documents` + CHECK
  - [x] `shared/routes.ts` `documents.list`: query-параметры `viewMode` (`project|global|all`), объект берётся из заголовка `X-Object-Id`/сессии
  - [x] `shared/routes.ts` `documents.create`: `object_id` проставляется сервером по `scope` (клиент scope не передаёт напрямую — см. UI)
    - 2026-07-24: `scope` временно оставлен в контракте `create` для совместимости до реализации backend/frontend задач `DOC-SCOPE-003`/`DOC-SCOPE-005`; `objectId` в input не принимается.
  - [x] Добавить контракт `documents.patch` (`PATCH /api/documents/:id`): title, docNumber, docDate, validFrom, validTo, fileUrl (без смены scope здесь)
  - [x] Добавить контракт `documents.setScope` ИЛИ включить смену scope в patch (уточнить) — «Сделать глобальным»/«В проект»
- **Зависимости**: DOC-SCOPE-001

### Задача: DOC-SCOPE-003 — Backend: фильтрация и CRUD по объекту
- **Статус**: Реализована
- **Приоритет**: Высокий
- **Описание**: Переписать выдачу и создание документов с учётом текущего объекта; добавить PATCH и корректное удаление проектных/глобальных.
- **Шаги выполнения**:
  - [x] `server/routes/materials.ts` `GET /api/documents`: добавить `...appAuth` + `resolveCurrentObject`, прокинуть `objectId` и `viewMode`
    - 2026-07-24: временный fallback `scope -> viewMode` удалён в рамках `DOC-SCOPE-BL-011`; list API принимает только `viewMode`.
  - [x] `storage.searchDocuments({ objectId, viewMode, query, docType })`: реализовать правило видимости (project/global/all)
  - [x] `storage.createDocument`: для `project` — проставить `object_id = currentObject`; для `global` — `NULL`
  - [x] `POST /api/documents`: определить scope из тела (сегмент UI) и подставить object_id на сервере
  - [x] Реализовать `PATCH /api/documents/:id` (доступ любому владельцу — решение №5; для проектного проверяется владение объектом)
  - [x] `storage.deleteDocument`: проектный — soft-delete в своём объекте; глобальный — soft-delete любым владельцем, но запрет при использовании в актах (понятная 409)
  - [x] Действие «Сделать глобальным» (доступно всем — решение №7): `scope→global`, `object_id→NULL` (bindings сохраняются)
  - [x] Для global НЕ копируем в проект — привязка по ссылке (Вариант 1)
- **Зависимости**: DOC-SCOPE-002
- **Проверка**: на новом объекте `GET /api/documents?viewMode=project` пуст; global видны везде

### Задача: DOC-SCOPE-004 — Frontend: хуки use-documents
- **Статус**: Реализована
- **Приоритет**: Высокий
- **Описание**: Обновить хуки под режимы просмотра, добавить edit/patch и корректную инвалидацию по объекту.
- **Шаги выполнения**:
  - [x] `useDocuments({ viewMode, query, docType })` вместо `scope`
    - 2026-07-24: временно поддержан legacy `scope` как alias для совместимости до `DOC-SCOPE-005`.
  - [x] Добавить текущий `objectId` в queryKey (инвалидация при смене объекта)
  - [x] `useCreateDocument`: принимает `viewMode`/`scope` из активного сегмента
  - [x] Добавить `useUpdateDocument` (PATCH)
  - [x] `useDeleteDocument`: работает из реестра, обрабатывает 409 (используется в актах)
  - [x] `useSetDocumentScope` (сделать глобальным)
- **Зависимости**: DOC-SCOPE-002

### Задача: DOC-SCOPE-005 — Frontend: страница /source/documents (3 сегмента + действия)
- **Статус**: Реализована
- **Приоритет**: Высокий
- **Описание**: Переработать реестр: сегменты Проект/Глобальные/Все, бейджи, действия create/edit/delete/setScope прямо в карточке.
- **Шаги выполнения**:
  - [x] Заменить select `scope` на сегменты **Проект: <объект> / Глобальные / Все**
  - [x] По умолчанию активен сегмент **Проект**
  - [x] В режиме «Все» на карточке — бейдж `Проект` / `Глобальный`
  - [x] Форма создания: scope определяется активным сегментом (в «Все» — явный выбор), поля `validFrom/validTo` (сейчас всегда null)
  - [x] Кнопки на карточке: Открыть, Редактировать, Удалить, «Сделать глобальным» (для проектного)
  - [x] Confirm перед удалением; обработка ошибки 409 (используется в актах) c подсказкой
  - [x] Пустые состояния для каждого сегмента
- **Зависимости**: DOC-SCOPE-004

### Задача: DOC-SCOPE-006 — Синхронизация связанных экранов
- **Статус**: Реализована
- **Приоритет**: Средний
- **Описание**: Привести счётчики и выбор документов к новой логике видимости.
- **Шаги выполнения**:
  - [x] `SourceData.tsx`: счётчик «Документы качества» = проектные текущего объекта **+ глобальные** (решение №6)
  - [x] `MaterialFullCard.tsx` / `MaterialWizard.tsx`: выбор существующего документа из «проект + глобальные», а не из всего реестра
  - [x] Проверить `Schedule.tsx` (статусы quality-доков) на согласованность
    - 2026-07-24: статусы Schedule считаются по уже созданным `document_bindings` и join на активные documents; отдельной правки под режимы видимости не требуется.
- **Зависимости**: DOC-SCOPE-004

### Задача: DOC-SCOPE-007 — Тесты и документация
- **Статус**: Реализована
- **Приоритет**: Средний
- **Описание**: Покрыть контракты и логику видимости; обновить проектную документацию.
- **Шаги выполнения**:
  - [x] Контракт-тесты: `documents.list` (viewMode), `documents.patch`, обновлённый `documents.delete`
  - [x] Тест видимости: новый объект не видит проектные документы другого объекта; global видны везде
    - 2026-07-24: покрыто implementation smoke-тестом `storage.searchDocuments` без поднятия Postgres.
  - [x] Тест миграции backfill (project → object_id)
    - 2026-07-24: покрыто SQL smoke-тестом миграции `0026_documents_object_id.sql`.
  - [x] `npm run check` + `npm run test`
  - [x] Обновить `docs/project.md` (модель документов, API), `docs/changelog.md`, `docs/tasktracker.md`
- **Зависимости**: DOC-SCOPE-003, DOC-SCOPE-005

---

## Открытые вопросы
Все ключевые вопросы согласованы (см. «Принятые решения» №4–№7). Открытых вопросов нет.

## Критерии готовности (Definition of Done)
- На новом объекте нет чужих документов; видны только его проектные + глобальные.
- Реестр поддерживает 3 режима с бейджами и действиями create/edit/delete/setScope.
- Глобальный нельзя удалить, пока используется в актах (понятная ошибка).
- Миграция безопасна, данные сохранены; тесты и `npm run check` зелёные.
- Документация (`project.md`, `changelog.md`, `tasktracker.md`) обновлена.

---

## Предложения улучшений (бэклог)

> Этот раздел не входит в исходный объём `DOC-SCOPE-001…007`. Задачи выполнять
> отдельными небольшими изменениями после стабилизации основной функциональности.
> Перед началом каждой задачи агент должен проверить актуальный код и не считать
> приведённые ниже пути и детали реализации неизменными.

### P0 — перед деплоем и для надёжности миграций

#### DOC-SCOPE-BL-001 — Применить и проверить миграцию `0026` во всех окружениях
- **Статус**: Локальная/CI-часть реализована; staging/production требуют доступа к БД и backup
- **Приоритет**: Критический
- **Цель**: Гарантировать, что код, использующий `documents.object_id`, не запускается на старой схеме.
- **Контекст для агента**:
  - Проверить local → staging → production именно в таком порядке.
  - Не выполнять production-деплой без резервной копии и проверки результата на staging.
  - Миграция должна быть идемпотентной и не должна повторно менять уже обработанные документы.
- **Шаги выполнения**:
  - [x] Проверить наличие колонки, FK, CHECK и индексов через системные таблицы PostgreSQL в CI workflow.
  - [x] Документировать preflight-запросы для количества активных документов по `scope` и осиротевших project-документов.
  - [x] Подготовить локальное применение `migrations/0026_documents_object_id.sql` через clean migration workflow.
  - [x] Проверить backfill/soft-delete smoke-тестами миграции.
  - [x] Выполнить smoke-сценарий видимости на уровне storage/contract тестов.
  - [ ] Повторить проверки на staging; production — только после подтверждения.
- **Критерии приёмки**:
  - Во всех целевых БД есть `documents.object_id`, FK `ON DELETE CASCADE`, CHECK и оба индекса.
  - Нет активных project-документов с `object_id IS NULL`.
  - API списка и создания документов не возвращает 500 из-за несовпадения схемы.
- **Зависимости**: завершённые `DOC-SCOPE-001…007`; доступ к БД окружений.

#### DOC-SCOPE-BL-002 — Починить чистую цепочку миграций на `0019`
- **Статус**: Реализована
- **Приоритет**: Высокий
- **Цель**: Сделать полный `test-migrations.yml` зелёным на пустой БД.
- **Контекст для агента**:
  - Причина: `0001_objects_source_data.sql` создаёт объект id=1 без владельца, а `0018` не назначает ему `user_id`; `0019` падает перед `SET NOT NULL`.
  - Рекомендуемое решение: идемпотентный backfill orphan-объектов в `0019` перед `SET NOT NULL`.
  - Не назначать объект случайному реальному пользователю; определить явную безопасную политику системного/legacy-владельца.
- **Шаги выполнения**:
  - [x] Воспроизвести причину падения по clean-chain анализу `0001` → `0018` → `0019`.
  - [x] Добавить безопасный backfill `objects.user_id IS NULL` через заблокированного `Legacy System Owner`.
  - [x] Сохранить идемпотентность повторного запуска миграции.
  - [x] Расширить workflow до последней миграции, включая `0026`/`0027` проверки.
  - [x] Добавить regression-проверку на orphan seed object в миграционную документацию/тесты.
- **Критерии приёмки**:
  - Полная цепочка миграций проходит на чистой БД.
  - После миграций `objects.user_id` имеет `NOT NULL`, orphan-объектов нет.
  - Поведение существующих production-данных не меняется.
- **Зависимости**: отдельная задача по техническому долгу миграций.

### P1 — корректность и понятный UX

#### DOC-SCOPE-BL-003 — Типизированная обработка ошибки 409 при удалении
- **Статус**: Реализована
- **Приоритет**: Высокий
- **Цель**: Показывать пользователю понятную причину отказа вместо сырого английского сообщения.
- **Контекст для агента**:
  - `useDeleteDocument` сейчас создаёт обычный `Error` без поля `status`.
  - Использовать тот же подход, что и в `useDeleteEstimate`: `Error & { status?: number }`.
- **Шаги выполнения**:
  - [x] Сохранить HTTP status в ошибке mutation.
  - [x] Для 409 показать русский текст: документ используется в актах и сначала нужно удалить/заменить его использование.
  - [x] Для 401/403/404 сохранить единообразную обработку API-ошибок.
  - [x] Добавить unit/contract-тест обработки 409.
- **Критерии приёмки**:
  - При 409 пользователь видит понятную инструкцию.
  - Диалог удаления остаётся управляемым, повторный запрос не отправляется автоматически.
- **Зависимости**: `useDeleteDocument`, `SourceDocuments.tsx`.

#### DOC-SCOPE-BL-004 — Согласовать редактирование типа документа
- **Статус**: Реализована
- **Приоритет**: Средний
- **Цель**: Устранить ситуацию, когда UI позволяет сменить тип, но PATCH его не сохраняет.
- **Рекомендуемое решение**: добавить `docType` в `documents.patch` и серверный whitelist.
- **Шаги выполнения**:
  - [x] Расширить Zod-контракт PATCH полем `docType`.
  - [x] Обновить `storage.updateDocument` и hook.
  - [x] Проверить CHECK `documents_doc_type_check`.
  - [x] Добавить тест успешной смены типа и отклонения неизвестного значения.
- **Критерии приёмки**:
  - Выбранный тип сохраняется после обновления страницы.
  - Некорректный тип возвращает 400.
- **Альтернатива**: сделать поле типа disabled при редактировании, если изменение типа бизнесу не требуется.

#### DOC-SCOPE-BL-005 — Подтверждение «Сделать глобальным»
- **Статус**: Реализована
- **Приоритет**: Средний
- **Цель**: Предотвратить случайное раскрытие проектного документа во всех объектах.
- **Шаги выполнения**:
  - [x] Перед mutation открыть confirm-dialog.
  - [x] Объяснить последствия: документ станет видимым во всех объектах, bindings сохранятся.
  - [x] Заблокировать кнопки на время запроса и исключить двойной submit.
  - [x] После успеха инвалидировать project/global/all query.
- **Критерии приёмки**:
  - Смена scope не выполняется без явного подтверждения.
  - После успеха документ сразу перемещается между сегментами.
- **Зависимости**: `SourceDocuments.tsx`, `useSetDocumentScope`.

#### DOC-SCOPE-BL-006 — Единая защита удаления документов, используемых в актах
- **Статус**: Реализована
- **Приоритет**: Высокий
- **Цель**: Не допускать soft-delete используемого документа независимо от scope.
- **Контекст для агента**:
  - Сейчас проверка `act_document_attachments` и `act_material_usages` выполняется только для global.
  - Также проверить `task_materials.quality_document_id` и другие FK/бизнес-ссылки.
- **Шаги выполнения**:
  - [x] Выделить общую функцию проверки использования документа.
  - [x] Применить её к project и global.
  - [x] Вернуть 409 с диагностикой без раскрытия чужих данных.
  - [x] Добавить тесты для project/global и каждого типа использования.
- **Критерии приёмки**:
  - Используемый документ нельзя удалить ни в одном scope.
  - Неиспользуемый документ удаляется через soft-delete.
- **Зависимости**: backend delete, контракт 409.

#### DOC-SCOPE-BL-007 — Политика bindings при soft-delete
- **Статус**: Реализована
- **Приоритет**: Средний
- **Цель**: Исключить накопление неочевидных активных связей на удалённые документы.
- **Рекомендуемое решение**: при soft-delete удалять `document_bindings`, но не исторические связи актов; операцию выполнять транзакционно.
- **Шаги выполнения**:
  - [x] Проанализировать, нужны ли bindings для аудита/restore.
  - [x] Зафиксировать решение в `docs/project.md`.
  - [x] Реализовать очистку bindings soft-deleted документов во всех запросах.
  - [x] Добавить тест удаления документа с несколькими bindings.
- **Критерии приёмки**:
  - Удалённый документ не влияет на статусы материалов и графика.
  - Нет dangling UI-связей.
- **Зависимости**: решение о будущем restore документов.

### P2 — развитие продукта

#### DOC-SCOPE-BL-008 — Уточнить смысл счётчика документов на `/source-data`
- **Статус**: Реализована
- **Приоритет**: Средний
- **Цель**: Не создавать впечатление, что все global-документы уже относятся к текущему проекту.
- **Варианты UX**:
  1. Два числа: «Проектные N · Глобальные M».
  2. Основной счётчик — только проектные/привязанные; global показывать отдельно.
  3. Оставить общий счётчик, но добавить понятную подпись.
- **Шаги выполнения**:
  - [x] Выбран вариант `Проектные N · Глобальные M`.
  - [x] Загрузить project/global отдельными queries.
  - [x] Обновить подпись.
- **Критерии приёмки**:
  - Пользователь понимает, какие документы принадлежат проекту, а какие доступны из общей библиотеки.

#### DOC-SCOPE-BL-009 — Фильтры и scope-бейджи в picker документов
- **Статус**: Реализована
- **Приоритет**: Средний
- **Цель**: Упростить выбор между проектными и глобальными документами при привязке к материалу.
- **Шаги выполнения**:
  - [x] В `MaterialFullCard` и `MaterialWizard` показывать одинаковый бейдж scope.
  - [x] Добавить фильтр `Все / Проект / Глобальные`.
  - [x] Сохранить поиск и исключить уже привязанные документы.
  - [x] Проверить мобильную прокрутку и empty states на уровне UI-структуры.
- **Критерии приёмки**:
  - Scope каждого документа виден до выбора.
  - Один документ нельзя ошибочно привязать повторно к той же цели.

#### DOC-SCOPE-BL-010 — Запретить неявный перенос global → project
- **Статус**: Реализована
- **Приоритет**: Средний
- **Цель**: Не допустить исчезновения общего документа из других объектов при вызове `setScope: project`.
- **Рекомендуемое решение**:
  - Основной UI поддерживает только project → global.
  - Обратное действие либо запрещено API, либо требует отдельной операции «Создать проектную копию».
- **Шаги выполнения**:
  - [x] Выбрать политику: запрет.
  - [x] Если запрет — сузить контракт endpoint и backend.
  - [x] Если копирование — не применяется.
  - [x] Добавить тест, что global не может быть демотирован в project.
- **Критерии приёмки**:
  - Существующий global-документ не может быть случайно «забран» одним проектом.

#### DOC-SCOPE-BL-011 — Удалить legacy alias `scope` из list API/hooks
- **Статус**: Реализована
- **Приоритет**: Низкий
- **Цель**: Оставить один понятный контракт фильтрации — `viewMode`.
- **Шаги выполнения**:
  - [x] Убедиться, что все клиенты используют `viewMode`.
  - [x] Удалить fallback `scope → viewMode` в route и hook.
  - [x] Обновить тесты и документацию.
- **Критерии приёмки**:
  - В коде списка документов нет legacy-параметра `scope`.
  - Create scope остаётся отдельным полем и не смешивается с view mode.

#### DOC-SCOPE-BL-012 — Улучшить названия сегментов
- **Статус**: Реализована
- **Приоритет**: Низкий
- **Цель**: Сделать текущий контекст объекта видимым прямо в переключателе.
- **Шаги выполнения**:
  - [x] Рассмотреть подпись `Проект: <краткое имя>` с ellipsis.
  - [x] На узком экране оставить короткий label и отдельную строку контекста.
  - [x] Добавить tooltip/full title для длинного имени.
- **Критерии приёмки**:
  - Пользователь всегда понимает, в каком объекте открыт проектный реестр.

#### DOC-SCOPE-BL-013 — Компактное меню действий карточки
- **Статус**: Реализована
- **Приоритет**: Низкий
- **Цель**: Уменьшить визуальную перегрузку карточки на мобильных устройствах.
- **Рекомендуемое решение**: оставить основное действие «Открыть», остальные поместить в меню `⋯`.
- **Шаги выполнения**:
  - [x] Проверить touch target минимум 44×44 px.
  - [x] Перенести Edit / Make global / Delete в dropdown.
  - [x] Сохранить aria-label и keyboard navigation.
- **Критерии приёмки**:
  - Карточка не переполняется на мобильном viewport.
  - Все действия доступны с клавиатуры и screen reader.

#### DOC-SCOPE-BL-014 — Аудит глобальных документов
- **Статус**: Реализована
- **Приоритет**: Низкий
- **Цель**: Сохранить ответственность за изменения при политике «любой владелец может редактировать global».
- **Шаги выполнения**:
  - [x] Добавить `created_by_user_id` и `updated_by_user_id`.
  - [x] Определить `ON DELETE SET NULL` и миграцию существующих строк.
  - [x] Сохранять автора/последнее изменение в записи документа.
  - [x] Не раскрывать лишние персональные данные в UI.
- **Критерии приёмки**:
  - Для глобального документа можно определить, кто его создал и менял.
- **Зависимости**: согласование требований аудита.

### P3 — тесты и поддерживаемость

#### DOC-SCOPE-BL-015 — Runtime integration-тесты видимости на PostgreSQL
- **Статус**: Частично реализована smoke/contract тестами; полноценный PostgreSQL runtime test требует отдельной CI-инфраструктуры
- **Приоритет**: Высокий
- **Цель**: Заменить regex-smoke реальной проверкой SQL/Storage.
- **Сценарий теста**:
  - Пользователь с объектами A и B.
  - Project-документ A, project-документ B, global-документ.
  - Проверить `project`, `global`, `all` для обоих объектов и исключение soft-deleted.
- **Шаги выполнения**:
  - [ ] Поднять изолированную тестовую БД.
  - [ ] Выполнить миграции/fixtures.
  - [ ] Вызвать storage или HTTP API с реальной аутентификацией.
  - [ ] Проверить отсутствие cross-object утечки.
- **Критерии приёмки**:
  - Тест падает при удалении object-фильтра.
  - Тест запускается локально и в CI.

#### DOC-SCOPE-BL-016 — Integration-тест миграции `0026`
- **Статус**: Частично реализована SQL smoke/CI schema checks; полноценный fixture-based PostgreSQL test требует отдельной CI-инфраструктуры
- **Приоритет**: Высокий
- **Цель**: Проверить реальный backfill, soft-delete orphan и CHECK.
- **Шаги выполнения**:
  - [ ] Создать pre-0026 fixture с bindings через object/material/batch.
  - [ ] Добавить документ с одной однозначной привязкой, orphan и конфликтом нескольких объектов.
  - [ ] Применить `0026`.
  - [ ] Проверить `object_id`, `deleted_at`, FK, индексы и невозможность нарушить CHECK.
- **Критерии приёмки**:
  - Backfill подтверждён данными, а не поиском текста SQL.
  - Повторное применение не повреждает данные.
- **Зависимости**: исправленный clean workflow (`DOC-SCOPE-BL-002`).

#### DOC-SCOPE-BL-017 — Проверить auth/ownership для document bindings
- **Статус**: Реализована
- **Приоритет**: Высокий
- **Цель**: Исключить создание/изменение binding к чужому объекту или материалу.
- **Контекст для агента**:
  - Проверить `POST/PATCH/DELETE /api/document-bindings`; не предполагать безопасность только из-за защищённого UI.
- **Шаги выполнения**:
  - [x] Добавить `appAuth` и object resolution ко всем mutation endpoints, где их нет.
  - [x] Проверить владение document, object, projectMaterial и batch.
  - [x] Для global разрешить ссылку в текущий объект; для чужого project-document вернуть 404.
  - [x] Добавить security smoke-тесты cross-object сценариев.
- **Критерии приёмки**:
  - Пользователь не может создать, изменить или удалить binding чужого объекта.
  - Ошибки не раскрывают существование чужих сущностей.
